
  # Generate Design Element (Copy)

  This is a code bundle for Generate Design Element (Copy). The original project is available at https://www.figma.com/design/FxrPKoNeJepSkNuaD2TMaC/Generate-Design-Element--Copy-.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  